[project]
name = "powerbi-translation-audit"
version = "0.1.0"
description = "MCP server for Power BI translation audit"
requires-python = ">=3.12"
dependencies = [
    "mcp",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
